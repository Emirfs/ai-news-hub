---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import LeadStory from '../components/LeadStory.astro';
import NewsCard from '../components/NewsCard.astro';

const allNews = await getCollection('news');

// Sort by date descending
const sortedNews = allNews.sort(
  (a, b) => b.data.pubDate.getTime() - a.data.pubDate.getTime()
);

const digests = sortedNews.filter((item) => item.data.isWeeklyDigest);
const regularArticles = sortedNews.filter((item) => !item.data.isWeeklyDigest);

const leadStory = regularArticles[0];
const gridArticles = regularArticles.slice(1);

const categories = Array.from(new Set(allNews.map((item) => item.data.category)));
---

<BaseLayout>
  <!-- Newsroom Status Bar -->
  <section class="mb-8 p-3.5 rounded bg-[#121214] border border-neutral-800 flex flex-wrap items-center justify-between gap-4 text-xs font-mono">
    <div class="flex items-center gap-6">
      <div class="flex items-center gap-2">
        <span class="w-2 h-2 rounded-full bg-emerald-400"></span>
        <span class="text-neutral-400">Newsroom Status:</span>
        <span class="text-neutral-200 font-semibold">Autonomous Active (Hourly)</span>
      </div>
      <div class="hidden sm:flex items-center gap-2">
        <span class="text-neutral-400">Model Engine:</span>
        <span class="text-neutral-200">Gemini Flash</span>
      </div>
    </div>
    <div class="text-neutral-400">
      Total Indexed Dispatches: <strong class="text-white">{allNews.length}</strong>
    </div>
  </section>

  <!-- Lead Story -->
  {leadStory && (
    <LeadStory
      title={leadStory.data.title}
      description={leadStory.data.description}
      slug={leadStory.id.replace(/\.md$/, '')}
      pubDate={leadStory.data.pubDate}
      category={leadStory.data.category}
      sourceName={leadStory.data.sourceName}
      keyTakeaways={leadStory.data.keyTakeaways}
    />
  )}

  <!-- Weekly Digest Spotlight (if any digest exists) -->
  {digests.length > 0 && (
    <section class="mb-12 p-6 rounded-lg bg-[#141416] border border-amber-500/30">
      <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-4 mb-4">
        <div class="flex items-center gap-2 font-mono text-sm font-bold text-amber-400 uppercase tracking-wider">
          <span>📋</span>
          Latest Executive Briefing
        </div>
        <a href="/digests" class="text-xs font-mono text-amber-400 hover:text-amber-300 flex items-center gap-1 font-semibold">
          View All Weekly Briefings →
        </a>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        {digests.slice(0, 2).map((digest) => (
          <a
            href={`/news/${digest.id.replace(/\.md$/, '')}`}
            class="p-4 rounded bg-[#0c0c0e] border border-neutral-800 hover:border-amber-500/40 transition-colors block group"
          >
            <div class="text-xs font-mono text-amber-400 mb-1">
              {new Intl.DateTimeFormat('en-US', { month: 'short', day: 'numeric', year: 'numeric' }).format(digest.data.pubDate)}
            </div>
            <h4 class="text-base font-bold text-white group-hover:text-amber-300 transition-colors line-clamp-2 font-newspaper">
              {digest.data.title}
            </h4>
            <p class="text-xs text-neutral-400 line-clamp-2 mt-2">
              {digest.data.description}
            </p>
          </a>
        ))}
      </div>
    </section>
  )}

  <!-- Category Filter Pill Bar -->
  <section class="mb-8 flex items-center gap-2 overflow-x-auto pb-2 scrollbar-none border-b border-neutral-800 pb-4">
    <span class="text-xs font-mono uppercase text-neutral-400 font-semibold mr-1 shrink-0">Filter Topic:</span>
    <a
      href="/"
      class="px-3 py-1 rounded text-xs font-mono font-medium bg-white text-black transition-colors shrink-0"
    >
      All Dispatches
    </a>
    {categories.map((cat) => (
      <a
        href={`/categories/${encodeURIComponent(cat)}`}
        class="px-3 py-1 rounded text-xs font-mono font-medium bg-neutral-800/80 hover:bg-neutral-700 text-neutral-300 hover:text-white border border-neutral-700/60 transition-colors shrink-0"
      >
        {cat}
      </a>
    ))}
  </section>

  <!-- News Grid -->
  <section>
    <div class="flex items-center justify-between mb-6 pb-2 border-b border-neutral-800">
      <h2 class="text-xl font-bold font-newspaper text-white tracking-wide uppercase flex items-center gap-2">
        <span class="text-amber-400">⚡</span> Frontier Dispatches
      </h2>
      <span class="text-xs font-mono text-neutral-400">
        Showing {gridArticles.length + (leadStory ? 1 : 0)} Reports
      </span>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {gridArticles.map((article) => (
        <NewsCard
          title={article.data.title}
          description={article.data.description}
          slug={article.id.replace(/\.md$/, '')}
          pubDate={article.data.pubDate}
          category={article.data.category}
          tags={article.data.tags}
          sourceName={article.data.sourceName}
          sourceUrl={article.data.sourceUrl}
          keyTakeaways={article.data.keyTakeaways}
          isWeeklyDigest={article.data.isWeeklyDigest}
        />
      ))}
    </div>
  </section>
</BaseLayout>
